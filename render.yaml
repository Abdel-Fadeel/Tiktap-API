services:
  - type: web
    name: tiktap-api
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5100
    healthCheckPath: /api/v1/health
    autoDeploy: true 